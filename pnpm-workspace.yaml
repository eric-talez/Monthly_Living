allowBuilds:
  '@parcel/watcher': true
  '@prisma/engines': true
  '@swc/core': true
  esbuild: true
  prisma: true
  sharp: true
  unrs-resolver: true

# 보안 advisory 대응 transitive override (Phase 1D-1)
# - postcss: next@16.2.10이 8.4.31을 번들 → GHSA-qx2v-qp2m-jg93(<8.5.10) 취약.
#   트리에 이미 존재하는 8.5.16으로 고정(전체 dedupe, build-time 전용).
# - @hono/node-server: prisma>@prisma/dev(dev 툴링)가 1.19.11 → GHSA-92pp-h63x-v22m(<1.19.13).
#   동일 minor 최신 1.19.14로 고정(앱 런타임 미도달).
overrides:
  postcss: 8.5.16
  '@hono/node-server': 1.19.14
